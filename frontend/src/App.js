import { useEffect, useState } from "react";
import axios from "axios";
import { BrowserRouter, Routes, Route, NavLink, Navigate } from "react-router-dom";
import { LayoutDashboard, Truck, Package, WalletCards, Users, Map, LogOut, Plus, Menu, X, Droplets, ArrowUpRight, AlertTriangle, CheckCircle2, Clock3, CircleDollarSign, BarChart3, GripVertical, Save } from "lucide-react";
import "@/App.css";
import "@/Operations.css";

const api=axios.create({baseURL:`${process.env.REACT_APP_BACKEND_URL}/api`});
const auth=()=>({headers:{Authorization:`Bearer ${localStorage.getItem("hydro_token")}`}});
const money=v=>new Intl.NumberFormat("pt-BR",{style:"currency",currency:"BRL"}).format(v||0);
const nav=[['/','Visão geral',LayoutDashboard],['/rotas','Rotas',Map],['/entregas','Entregas',Truck],['/estoque','Estoque',Package],['/financeiro','Financeiro',WalletCards],['/clientes','Clientes',Users],['/relatorios','Relatórios',BarChart3]];

function Shell({user,onLogout,children}){const [open,setOpen]=useState(false);const links=user.role==='driver'?nav.filter(x=>['/','/rotas','/entregas','/financeiro'].includes(x[0])):nav;return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><span className="brand-mark"><Droplets size={20}/></span><span>hydro<span>flow</span></span></div><div className="workspace"><small>OPERAÇÃO</small><b>Base Central <span>⌄</span></b></div><nav>{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} onClick={()=>setOpen(false)} data-testid={`nav-${label.toLowerCase().replaceAll(' ','-')}`}><Icon size={18}/>{label}</NavLink>)}</nav><div className="sidebar-bottom"><div className="support"><span className="live-dot"/> Operação normal</div><button className="logout" data-testid="logout-button" onClick={onLogout}><LogOut size={17}/> Sair da conta</button></div></aside><main className="main"><header><button className="mobile-menu" data-testid="mobile-menu-button" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button><div><p className="eyebrow">{user.role==='driver'?'PORTAL DO ENTREGADOR':'TERÇA-FEIRA, 18 JUN 2024'}</p><h1>Olá, {user.name.split(' ')[0]} <span className="wave">✦</span></h1></div><div className="header-actions"><div className="notification" data-testid="notification-indicator"><span>3</span>◌</div><div className="avatar">{user.name.split(' ').map(x=>x[0]).join('').slice(0,2)}</div></div></header>{children}</main></div>}
function Login({onLogin}){const [email,setEmail]=useState('admin@hydroflow.com'),[password,setPassword]=useState('admin123'),[error,setError]=useState('');async function submit(e){e.preventDefault();try{const {data}=await api.post('/auth/login',{email,password});localStorage.setItem('hydro_token',data.token);onLogin(data)}catch(e){setError(e.response?.data?.detail||'Não foi possível entrar')}}return <div className="login-page"><div className="login-art"><div className="login-brand"><Droplets/> hydro<span>flow</span></div><div className="login-quote">Água em movimento.<br/><em>Operação sob controle.</em></div></div><form className="login-form" onSubmit={submit}><p className="eyebrow">BEM-VINDO DE VOLTA</p><h1>Acesse sua operação</h1><p className="muted">Entre para acompanhar tudo que acontece na sua base.</p><label>E-mail<input data-testid="login-email-input" type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input data-testid="login-password-input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<div className="error" data-testid="login-error">{error}</div>}<button className="primary full" data-testid="login-submit-button">Entrar na plataforma <ArrowUpRight size={17}/></button><div className="demo-hint">Admin: admin@hydroflow.com / admin123<br/>Entregador: carlos@hydroflow.com / driver123</div></form></div>}
function Head({eyebrow,title,subtitle,action,onAction}){return <section className="section-head"><div><p className="eyebrow">{eyebrow}</p><h2>{title}</h2><p className="muted">{subtitle}</p></div>{action&&<button className="primary" onClick={onAction} data-testid={`new-${title.toLowerCase().replaceAll(' ','-')}-button`}><Plus size={17}/>{action}</button>}</section>}
function Stat({label,value,detail,Icon,tone=''}){return <div className="stat" data-testid={`stat-${label.toLowerCase().replaceAll(' ','-')}`}><div className={`stat-icon ${tone}`}><Icon size={19}/></div><div><p>{label}</p><strong>{value}</strong><small>{detail}</small></div></div>}
function Modal({title,fields,onClose,onSave}){const [form,setForm]=useState({}),[error,setError]=useState('');async function submit(e){e.preventDefault();if(fields.some(x=>x.required!==false&&!String(form[x.key]||'').trim()))return setError('Preencha os campos obrigatórios.');try{await onSave(form)}catch(e){setError(e.response?.data?.detail||'Não foi possível salvar.')}}return <div className="modal-backdrop"><form className="quick-modal" onSubmit={submit}><button type="button" className="modal-close" onClick={onClose} data-testid="modal-close-button"><X/></button><p className="eyebrow">NOVO LANÇAMENTO</p><h3>{title}</h3>{fields.map(f=><label key={f.key}>{f.label}{f.options?<select required={f.required!==false} data-testid={`modal-${f.key}-input`} onChange={e=>setForm({...form,[f.key]:e.target.value})}><option value="">Selecione</option>{f.options.map(x=><option key={x}>{x}</option>)}</select>:<input required={f.required!==false} type={f.type||'text'} data-testid={`modal-${f.key}-input`} onChange={e=>setForm({...form,[f.key]:e.target.value})}/>}</label>)}{error&&<div className="error" data-testid="form-validation-error">{error}</div>}<button className="primary full" data-testid="modal-submit-button"><Save size={16}/> Salvar lançamento</button></form></div>}
const fields={product:[{key:'name',label:'Nome do produto'},{key:'category',label:'Categoria',options:['Retornável','Descartável']},{key:'quantity',label:'Quantidade',type:'number'},{key:'minimum',label:'Estoque mínimo',type:'number'}],delivery:[{key:'customer',label:'Cliente'},{key:'address',label:'Endereço'},{key:'driver',label:'Entregador'},{key:'product',label:'Produto'},{key:'quantity',label:'Quantidade',type:'number'},{key:'value',label:'Valor',type:'number'}],expense:[{key:'type',label:'Categoria',options:['Combustível','Pedágio','Manutenção','Outros']},{key:'driver',label:'Entregador'},{key:'amount',label:'Valor',type:'number'}],customer:[{key:'name',label:'Nome / empresa'},{key:'address',label:'Endereço'},{key:'phone',label:'Telefone',required:false}]};
function Dashboard({data,create}){return <><Head eyebrow="PAINEL DE CONTROLE" title="Visão geral" subtitle="Acompanhe a saúde da sua operação em um só lugar." action="Nova entrega" onAction={()=>create('delivery')}/><div className="stats"><Stat label="Receita no mês" value={money(data?.revenue)} detail="Receitas concluídas" Icon={CircleDollarSign}/><Stat label="Despesas pendentes" value={money(data?.expenses)} detail="Aguardando aprovação" Icon={WalletCards} tone="orange"/><Stat label="Entregas hoje" value={data?.deliveries?.length||0} detail="Rotas em acompanhamento" Icon={Truck} tone="green"/><Stat label="Alertas de estoque" value={data?.products?.filter(x=>x.quantity<x.minimum).length||0} detail="Itens abaixo do mínimo" Icon={AlertTriangle} tone="red"/></div><div className="dashboard-grid"><section className="panel performance"><div className="panel-head"><div><h3>Desempenho financeiro</h3><p className="muted">Visão acumulada da operação</p></div><BarChart3 className="blue-text"/></div><div className="chart"><div className="bars">{[45,62,54,78,66,92].map((h,i)=><div className="bar-group" key={i}><div className="bar revenue" style={{height:`${h}%`}}/><div className="bar expense" style={{height:`${h*.3}%`}}/><span>{['Jan','Fev','Mar','Abr','Mai','Jun'][i]}</span></div>)}</div></div></section><section className="panel route-panel"><div className="panel-head"><div><h3>Rotas de hoje</h3><p className="muted">Status da operação</p></div><Map size={20} className="blue-text"/></div>{['Rota Norte · Carlos Mendes','Rota Centro · Ana Souza','Rota Sul · João Lima'].map((x,i)=><div className="route" key={x}><div className={`route-icon ${i===0?'green-bg':i===1?'blue-bg':'gray-bg'}`}>{i===0?<CheckCircle2 size={18}/>:i===1?<Truck size={18}/>:<Clock3 size={18}/>}</div><div><b>{x}</b><small>{i===0?'8 de 8 paradas concluídas':i===1?'4 de 7 paradas concluídas':'6 paradas programadas'}</small></div><span className={`tag ${i===0?'green':i===1?'blue':'gray'}`}>{i===0?'Concluída':i===1?'Em rota':'Pendente'}</span></div>)}</section></div></>}
function Deliveries({data,setData,create}){const [filter,setFilter]=useState('all');const rows=(data?.deliveries||[]).filter(x=>filter==='all'||x.status===filter);async function change(d,status){await api.patch(`/deliveries/${d.id}`,{status},auth());setData({...data,deliveries:data.deliveries.map(x=>x.id===d.id?{...x,status}:x)})}return <><Head eyebrow="LOGÍSTICA" title="Entregas" subtitle="Acompanhe cada entrega e status da rota." action="Lançar entrega" onAction={()=>create('delivery')}/><div className="filter-row"><button className={filter==='all'?'active':''} onClick={()=>setFilter('all')} data-testid="delivery-filter-all">Todas</button><button className={filter==='pending'?'active':''} onClick={()=>setFilter('pending')} data-testid="delivery-filter-pending">Pendentes</button><button className={filter==='in_transit'?'active':''} onClick={()=>setFilter('in_transit')} data-testid="delivery-filter-in-transit">Em rota</button></div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>CLIENTE</th><th>PRODUTO</th><th>ENTREGADOR</th><th>VALOR</th><th>STATUS</th></tr></thead><tbody>{rows.map(d=><tr key={d.id}><td><b>{d.customer}</b><small>{d.address}</small></td><td>{d.product}<small>{d.quantity} unidades</small></td><td>{d.driver}</td><td>{money(d.value)}</td><td><select data-testid={`delivery-status-${d.id}`} value={d.status} onChange={e=>change(d,e.target.value)}><option value="pending">Pendente</option><option value="in_transit">Em rota</option><option value="delivered">Entregue</option><option value="failed">Não realizada</option><option value="damaged">Avaria</option></select></td></tr>)}</tbody></table></div></section></>}
function RoutesPage({data,setData}){const [stops,setStops]=useState(data?.deliveries||[]);useEffect(()=>setStops(data?.deliveries||[]),[data]);function move(i,d){const a=[...stops],j=i+d;if(j>=0&&j<a.length)[a[i],a[j]]=[a[j],a[i]];setStops(a)}return <><Head eyebrow="PLANEJAMENTO" title="Rotas" subtitle="Ordene as paradas e prepare a operação do dia." action="Salvar ordem" onAction={()=>setData({...data,deliveries:stops})}/><div className="route-builder"><section className="panel stop-list"><div className="panel-head"><div><h3>Rota Centro · 18 junho</h3><p className="muted">Sequência de paradas</p></div><span className="tag blue">{stops.length} paradas</span></div>{stops.map((s,i)=><div className="stop-row" key={s.id}><GripVertical size={18} className="grip"/><span className="stop-number">{i+1}</span><div><b>{s.customer}</b><small>{s.address}</small></div><div className="stop-actions"><button data-testid={`route-up-${s.id}`} onClick={()=>move(i,-1)}>↑</button><button data-testid={`route-down-${s.id}`} onClick={()=>move(i,1)}>↓</button></div></div>)}</section><section className="panel map-preview"><div className="map-grid"><Map size={45}/><div className="map-pin pin-a">1</div><div className="map-pin pin-b">2</div><div className="map-pin pin-c">3</div><svg viewBox="0 0 300 200"><path d="M40 140 C100 30 140 190 250 55"/></svg></div><div className="map-caption"><b>Visão da rota</b><span>Centro · 12,4 km estimados</span></div></section></div></>}
function Stock({data,create}){return <><Head eyebrow="INVENTÁRIO" title="Estoque" subtitle="Produtos, galões retornáveis e níveis mínimos." action="Novo produto" onAction={()=>create('product')}/><div className="stock-alert" data-testid="stock-alert"><AlertTriangle size={19}/><div><b>{data?.products?.filter(x=>x.quantity<x.minimum).length||0} produtos precisam de reposição</b><span>Confira os itens antes da próxima rota.</span></div></div><section className="panel table-panel"><div className="table-wrap"><table><thead><tr><th>PRODUTO</th><th>CATEGORIA</th><th>DISPONÍVEL</th><th>MÍNIMO</th><th>SITUAÇÃO</th></tr></thead><tbody>{(data?.products||[]).map(p=><tr key={p.id}><td><b>{p.name}</b><small>SKU-{p.id}</small></td><td>{p.category}</td><td>{p.quantity} {p.unit||'un'}</td><td>{p.minimum}</td><td><span className={`tag ${p.quantity<p.minimum?'red':'green'}`}>{p.quantity<p.minimum?'Repor':'Saudável'}</span></td></tr>)}</tbody></table></div></section></>}
function Finance({data,create}){return <><Head eyebrow="CONTROLE FINANCEIRO" title="Financeiro" subtitle="Recebimentos e despesas lançados pela equipe." action="Lançar despesa" onAction={()=>create('expense')}/><div className="stats"><Stat label="Recebido hoje" value={money(486)} detail="Entregas confirmadas" Icon={CircleDollarSign}/><Stat label="A aprovar" value={money(data?.expenses)} detail="Despesas pendentes" Icon={Clock3} tone="orange"/><Stat label="Saldo líquido" value={money((data?.revenue||0)-(data?.expenses||0))} detail="Período atual" Icon={ArrowUpRight} tone="green"/></div><section className="panel table-panel"><div className="panel-head"><div><h3>Movimentações recentes</h3><p className="muted">Conferência por entregador</p></div></div><div className="table-wrap"><table><thead><tr><th>TIPO</th><th>ENTREGADOR</th><th>VALOR</th><th>STATUS</th></tr></thead><tbody>{(data?.expenses_list||[]).map(e=><tr key={e.id}><td><b>{e.type}</b></td><td>{e.driver}</td><td>{money(e.amount)}</td><td><span className="tag orange">Aguardando aprovação</span></td></tr>)}</tbody></table></div></section></>}
function Customers({items,create}){return <><Head eyebrow="RELACIONAMENTO" title="Clientes" subtitle="Sua carteira e endereços de entrega." action="Novo cliente" onAction={()=>create('customer')}/><div className="customer-grid">{items.map(x=><div className="customer-card" key={x.id}><div className="customer-avatar">{x.name?.[0]}</div><div><b>{x.name}</b><span>{x.address}</span><small>{x.phone||'Cliente ativo'}</small></div><ArrowUpRight size={18}/></div>)}</div></>}
function Reports(){const [r,setR]=useState(null);useEffect(()=>{api.get('/reports',auth()).then(x=>setR(x.data)).catch(()=>setR({revenue:0,expenses:0,deliveries:0,low_stock:0,drivers:[]}))},[]);return <><Head eyebrow="INTELIGÊNCIA" title="Relatórios" subtitle="Indicadores para decidir melhor a cada período."/><div className="report-toolbar"><span>Período analisado</span><select data-testid="report-period-select"><option>Últimos 30 dias</option><option>Últimos 3 meses</option></select><button data-testid="export-report-button" className="primary"><ArrowUpRight size={16}/> Exportar resumo</button></div><div className="stats"><Stat label="Receita realizada" value={money(r?.revenue)} detail="Entregas concluídas" Icon={CircleDollarSign}/><Stat label="Despesas" value={money(r?.expenses)} detail="Lançamentos" Icon={WalletCards} tone="orange"/><Stat label="Entregas" value={r?.deliveries||0} detail="No período" Icon={Truck} tone="green"/><Stat label="Alertas estoque" value={r?.low_stock||0} detail="Atenção necessária" Icon={AlertTriangle} tone="red"/></div><section className="panel table-panel"><div className="panel-head"><div><h3>Desempenho por entregador</h3><p className="muted">Volume, receita e conclusão</p></div></div><div className="table-wrap"><table><thead><tr><th>ENTREGADOR</th><th>ENTREGAS</th><th>CONCLUÍDAS</th><th>RECEITA</th><th>EFICIÊNCIA</th></tr></thead><tbody>{(r?.drivers||[]).map(d=><tr key={d.driver}><td><b>{d.driver}</b></td><td>{d.deliveries}</td><td>{d.delivered}</td><td>{money(d.revenue)}</td><td><span className="tag green">{d.deliveries?Math.round(d.delivered/d.deliveries*100):0}%</span></td></tr>)}</tbody></table></div></section></>}
function App(){const [user,setUser]=useState(null),[data,setData]=useState(null),[customers,setCustomers]=useState([]),[checking,setChecking]=useState(true),[modal,setModal]=useState(null);useEffect(()=>{const t=localStorage.getItem('hydro_token');if(t)api.get('/auth/me',auth()).then(x=>setUser(x.data)).catch(()=>localStorage.removeItem('hydro_token')).finally(()=>setChecking(false));else setChecking(false)},[]);useEffect(()=>{if(user)Promise.all([api.get('/dashboard',auth()),api.get('/customers',auth())]).then(([a,c])=>{setData(a.data);setCustomers(c.data)})},[user]);if(checking)return <div className="loading">Carregando operação...</div>;if(!user)return <Login onLogin={setUser}/>;const logout=()=>{localStorage.removeItem('hydro_token');setUser(null)};async function save(kind,form){const endpoints={product:'/products',delivery:'/deliveries',expense:'/expenses',customer:'/customers'};const payload={...form};['quantity','minimum','value','amount'].forEach(k=>{if(payload[k]!==undefined)payload[k]=Number(payload[k])});if(kind==='delivery')payload.status='pending';const {data:x}=await api.post(endpoints[kind],payload,auth());if(kind==='customer')setCustomers([...customers,x]);else{const key=kind==='product'?'products':kind==='delivery'?'deliveries':'expenses_list';setData({...data,[key]:[...(data?.[key]||[]),x]})}setModal(null)}return <Shell user={user} onLogout={logout}><Routes><Route path="/" element={<Dashboard data={data} create={setModal}/>}/><Route path="/rotas" element={<RoutesPage data={data} setData={setData}/>}/><Route path="/entregas" element={<Deliveries data={data} setData={setData} create={setModal}/>}/><Route path="/estoque" element={<Stock data={data} create={setModal}/>}/><Route path="/financeiro" element={<Finance data={data} create={setModal}/>}/><Route path="/clientes" element={<Customers items={customers} create={setModal}/>}/><Route path="/relatorios" element={user.role==='admin'?<Reports/>:<Navigate to="/" replace/>}/></Routes>{modal&&<Modal title={{product:'Cadastrar produto',delivery:'Lançar entrega',expense:'Lançar despesa',customer:'Cadastrar cliente'}[modal]} fields={fields[modal]} onClose={()=>setModal(null)} onSave={x=>save(modal,x)}/>}</Shell>}
export default ()=> <BrowserRouter><App/></BrowserRouter>;